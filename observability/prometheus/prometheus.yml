global:
  scrape_interval: 10s

scrape_configs:
  - job_name: media-server
    metrics_path: /metrics
    static_configs:
      - targets:
          - media-server:3000
  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090
